#include <stdio.h>
#include<math.h>

int main() {

    int age = 22;
    int oldAge = age;
    int newAge = oldAge + 2;
       printf("new age is : %d", newAge);
    int rupee = 1, dollar;
    dollar = 74; 

    return 0;
}
