#include <stdio.h>
#include <math.h>

int main() {

    int number;
    printf("Write a number :");
    scanf("%d",&number);

    number >= 1 ? printf("It is a natural number") : printf("It is not a natural number");
    
   
    
    return 0;
}
